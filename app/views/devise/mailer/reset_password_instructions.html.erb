<p><%= @resource.email %>様</p>

<p>パスワードを変更するためのリンクを記載します。</p>

<p><%= link_to 'パスワードを変更する', edit_user_password_url(@resource, reset_password_token: @token) %></p>

<p>もしこのメールに覚えがない場合は、このメールを削除してください。</p>
<p>上記のリンクに遷移後、パスワードを再設定するまではパスワードが変更されることはありません。</p>