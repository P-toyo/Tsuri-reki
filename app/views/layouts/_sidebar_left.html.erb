<div class="col-2 border">
  <% @sidebar_posts.each do |post| %>
    <div class="text-truncate"><%= post.title %></div>
    <div class="text-truncate"><%= post.species.name %></div>
    <div class="border-bottom text-truncate"><%= post.created_at.strftime('%Y/%m/%d') %></div>
  <% end %>
</div>